<%= form_for :publish,
      :url => book_sales_account_type_create_publish_path do |f| %>
    <%= f.label :name, t("view.name") %>
    <%= f.text_field :name %>
    <br>

    <%= f.submit t("view.create") %>
    <br>
<% end %>

